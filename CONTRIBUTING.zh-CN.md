# 贡献指南

[English](CONTRIBUTING.md)

1. 新功能或兼容性变更先提 issue，说明目标并提供最小合成例子；小型修正可直接提 PR。
2. 创建分支和虚拟环境，安装 `pip install -e '.[code,dev]'`。
3. 先写失败的行为测试，再实现修正并执行 `scripts/check.sh`；Node 入口运行 `npm test`。
4. 用户可见变更同步更新中英文文档，并加入更新记录。
5. 语义或运行时变更附 A/B 证据，保留失败，区分离线规划和真实模型实验。

PR 标题使用 `fix:`、`feat:`、`docs:`、`test:`、`chore:` 等约定前缀。主分支受保护，必须通过检查，
禁止强推和删除。单维护者阶段要求 PR 与检查，不设置无法完成的第二人批准；外部贡献由维护者审核。

不要提交客户数据、私有提示词、token、浏览器状态、个人目录路径或未脱敏轨迹。
安全问题走 [私密报告](SECURITY.zh-CN.md)。贡献按 MIT 许可提供。

支持 Linux/macOS、Python 3.10+；Windows 使用 WSL。源码安装的搜索需要 rg，可选解析器使用
`code` 依赖；npm 平台包包含这些运行依赖。浏览器定位需要已有本地 Camofox 服务，测试使用受控页面。
